import { CreateRoomPageComponent } from "../features/Room/page-components/CreateRoomPage";
import {
  View,
  TextInput,
  Text,
  TouchableOpacity,
  TouchableWithoutFeedback,
  Keyboard,
} from "react-native";

export default function CreateRoomPage() {
  return <CreateRoomPageComponent />;
}
